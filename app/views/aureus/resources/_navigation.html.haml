- content_for :navigation do
  = aureus_navigation do |n|
    - n.title @aureus_title
    - @aureus_buttons.each do |button|
      - n.button link_to(*button)
    - #n.submit_form_button @user, 'Save'
